
	;; -*- asm -*-

#if 0 && SWUART_AUTOBAUD == 5
	.global swuart_sync
	swuart_sync = sync5x
#include "swuart-sync-5x.s"
#endif

	.global swuart_sync
	swuart_sync = syncd

	.global	swuart_chksync
	swuart_chksync = chksync3

#include "swuart-sync-div.sx"
